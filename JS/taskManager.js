  class TaskManager {
    constructor(currentId = 0) {
      this.currentId = currentId;
      this.tasks = [];    
    this.tasks.push('id:', 'name:', 'description: ', 'assignTo:', 'dueDate:', 'status:')
    }  
      getTaskById(){
        for (let index = 0; index < this.task.length; index++) {
	let tasks = array[index];
	tasks
}; newMethod().tasks = [];
        let arrayLength = this.tasks.length;
        for (let i = 0; i < this.tasks.Length; i++) {
            console.log(this.tasks[i]);
        }
      }
    newMethod() {
      return this;
    }

      addTask(name, description, assignedTo, dueDate, status) {
        this.tasks.push({
          currentId: this.currentId,
          name: name,
          description: description,
          assignedTo: assignedTo,
          dueDate: dueDate,
          status: "TODO",
        });
        this.currentId++;
      }
  }
function createTaskHtml (taskname, taskid, taskdescription, taskassignedTo, taskdueDate, taskstatus) {
  <button type = "button" class = "done-button" > Mark as Done </button>
     const html = `
          <li class="list-group-item">
              <div class="d-flex w-100 mt-2 justify-content-between align-items-center">
                  <h5>Please add a task name :${name}</h5>
                  <span class="badge badge-danger">TODO</span>
              </div>
              <div class="d-flex w-100 mb-3 justify-content-between">
                  <small>Assigned To: ${assignedTo}</small>
                  <small>Due date: ${dueDate}</small>
                  <small>Status of the task: ${status}</small>
                  <small>Id:${id}</small>
              </div>
              <p>${description}</p>
          </li>
      `}     
    render() ;{
      const tasksHtmlList = []
       for (let i = 0; i < this.task.length; i++) {
      const task = this.tasks[i]
      const formattedDate = date.prototype.getdueDate
      const taskHtml = createTaskHtml(task.id, task.name, task,description, task.assignedTo, formattedDate, task.status)
      tasksHtmlList.push(taskHtml)
} 
}
const tasksHtmlList = [document.getElementById("task").innerHTML, document.getElementById("description").innerHTML, document.getElementById("assignTo").innerHTML, document.getElementById("dueDate").innerHTML, "status"];
let foundTask = taskid.array